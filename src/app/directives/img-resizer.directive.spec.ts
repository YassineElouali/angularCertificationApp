import { ImgResizerDirective } from './img-resizer.directive';

describe('ImgResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgResizerDirective();
    expect(directive).toBeTruthy();
  });
});
